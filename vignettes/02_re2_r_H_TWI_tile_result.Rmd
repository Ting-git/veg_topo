---
title: "Pearson's r (H~TWI) Calculation by Tile"
author: "Ting Tan"
date: "`r Sys.Date()`"
output: html_document
---

## Task

The Pearson correlation analysis between vegetation height and topographic wetness index for all 49 tiles has been completed in `~/veg_topo/analysis/02_3_global_twi_vegh_cor.R`.

This markdown file focuses on visualizing the results, generating plots for each tile for easier inspection and interpretation.

```{r}

# list all figures of global correlation (H~TWI) for all 49 tiles
files <- list.files(path = here::here("data/figures"), pattern = "^02_tile.*_H_TWI_plots\\.png$", full.names = TRUE)

# Print 
cat("Found", length(files), "figures:\n")
# print(files)
```

```{r PrintPlot}
# print
knitr::include_graphics(files)
```


```{r, include=FALSE, eval=FALSE}
# This code will neither run nor show in the output
rmarkdown::render(here::here("vignettes/02_re2_r_H_TWI_tile_result.Rmd.Rmd"))
```

