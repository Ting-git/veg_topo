---
title: "Global correlation (H~Rin) results by tiles"
author: "Ting Tan"
date: "`r Sys.Date()`"
output: html_document
---


```{r SetUp, include=FALSE, eval=TRUE}

library(terra)

# Automatically select configuration file
hostname <- trimws(tolower(system("hostname", intern = TRUE)))
if (hostname == "dash") {
  message("💻 Detected Worksation: dash → using config.R")
  source(here::here("config.R"))
} else {
  message("🖥️ Detected HPC environment (", hostname, ") → using config_ubelix.R")
  source(here::here("config_ubelix.R"))
}
```

---

## Task

The Pearson correlation analysis between vegetation height and radiation index for all 49 tiles has been completed in `~/veg_topo/analysis/05_4_global_r_H_R_5km.R` and `~/veg_topo/analysis/05_5_plot_global_r_H_R_map.R`.

This markdown file focuses on visualizing the plots from scripts.

---

### Global map

```{r}
knitr::include_graphics(here::here("data/figures/05_r_H_R_5km_map.png"))
knitr::include_graphics(here::here("data/figures/05_r_H_R_5km_pval.png"))
```

## Pearson Correlation (H~Rin) for 49 Tiles

```{r ls-figures}
# list all figures of global correlation (H~TWI) for all 49 tiles
files <- list.files(path = here::here("data/figures"), pattern = "^05_tile.*_H_Rin_plots\\.png$", full.names = TRUE)

# Print 
cat("Found", length(files), "figures:\n")
# print(files)
```

```{r print}
knitr::include_graphics(files)
```

```{r, include=FALSE, eval=FALSE}
# This code will neither run nor show in the output
rmarkdown::render(here::here("/vignettes/05_re5_r_H_Rin_tile_result.Rmd"))
```
